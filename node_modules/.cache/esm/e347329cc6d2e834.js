let express,path,models,mongoose;_c6d‍.w("express",[["default",["express"],function(v){express=v}]]);_c6d‍.w("path",[["default",["path"],function(v){path=v}]]);_c6d‍.w("./models",[["default",["models"],function(v){models=v}]]);_c6d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);require("dotenv").config();





const app = express();
const PORT = process.env.PORT || 3000;

// Middleware
app.use(express.urlencoded({ extended: false }));
app.use(express.json());
app.use(express.static(path.join(__dirname, "/public")));


// Routes
require("./routes/apiRoutes")(app);
require("./routes/htmlRoutes")(app);

const syncOptions = { force: false };

// If running a test, set syncOptions.force to true
// clearing the `testdb`
if (process.env.NODE_ENV === "test") {
  syncOptions.force = true;
}

// Starting the server, syncing our models ------------------------------------/
connectDb().then(async () => {
  app.listen(PORT, () => {
    _c6d‍.g.console.log(
      "==>  Listening on port %s. Visit http://localhost:%s/ in your browser.",
      PORT,
      PORT
    );
  });
});
module.exports = app;
